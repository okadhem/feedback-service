spring:
  application:
    name: service-feedback
  cloud:
    config:
      uri: http://localhost:8888
  profiles:
    active: dev

management:
  security:
    enabled: false
      
      
      